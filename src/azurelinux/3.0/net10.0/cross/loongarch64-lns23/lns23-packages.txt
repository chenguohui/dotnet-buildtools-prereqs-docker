# Loongnix Server 23 sysroot package list (loongarch64).
# install-rpms.py does not resolve dependencies, so all required packages must
# be listed here. Keep in sync with the native build environment.

# --- Core system libraries ---
glibc
glibc-common
glibc-devel
glibc-static
kernel-headers

# --- Build toolchain (target side) ---
binutils
binutils-devel
gcc
gcc-c++
libgcc
libstdc++
libstdc++-devel
libstdc++-static
make
cmake
pkgconf
pkgconf-pkg-config

# --- Compression ---
zlib-devel
zlib-static
bzip2-devel
xz-devel

# --- Crypto / SSL ---
openssl-devel
openssl-libs

# --- Kerberos ---
krb5-devel
krb5-libs
libcom_err-devel

# --- HTTP / networking ---
libcurl-devel
libcurl
libbrotli
libxml2-devel

# --- ICU (internationalization) ---
libicu
libicu-devel

# --- LTTng (runtime event tracing) ---
lttng-ust
lttng-ust-devel

# --- Unwind (exception handling / stack unwinding) ---
libunwind
libunwind-devel

# --- OpenMP ---
libomp
libomp-devel

# --- ncurses ---
ncurses
ncurses-devel
ncurses-libs

# --- ELF / DWARF ---
elfutils-devel
elfutils-libelf-devel
elfutils-libs

# --- readline ---
readline-devel

# --- Transitive runtime libraries ---
# install-rpms.py has no dependency resolution, so the NEEDED chains of the
# -devel packages must be listed explicitly (names verified against the lns23
# os repo: note keyutils/lz4, not keyutils-libs/lz4-libs as on other distros).
zlib
bzip2-libs
xz-libs
libxml2
readline
libxcrypt
# krb5 chain
libverto
keyutils
libcom_err
libselinux
libsepol
# libcurl chain
libpsl
libidn2
libnghttp2
libssh
lz4
pcre2
openldap
libzstd
# lttng-ust chain
userspace-rcu
userspace-rcu-devel
systemtap-sdt-devel
libatomic
